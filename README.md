# ColourShift

## Install
```bash
uv sync
```

## Run
```bash
uv run colourshift
```

The legacy runner still works:

```bash
uv run python colourshift.py
```

## Development
```bash
uv sync
uv run pytest
uv run ruff check .
uv run ruff format .
```
